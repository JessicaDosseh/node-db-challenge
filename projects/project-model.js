const db = require('../data/db-config.js'); 

module.exports = {
  getProjects,
  getProjectsById,
  addProjects,
  updateProjects,
  removeProjects,
};


// ----------------------------------------------

// CRUD operations for Projects: 
